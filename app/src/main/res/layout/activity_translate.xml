<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/bg_base">

    <include layout="@layout/view_toolbar"/>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="none">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="@dimen/gutter">

            <include
                android:id="@+id/translate_language_bar"
                layout="@layout/view_language_bar"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"/>

            <!-- Source input -->
            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/gutter_small"
                android:background="@drawable/bg_surface_card">

                <EditText
                    android:id="@+id/translate_input"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:minHeight="120dp"
                    android:padding="14dp"
                    android:paddingEnd="44dp"
                    android:background="@null"
                    android:gravity="top|start"
                    android:hint="@string/translate_hint"
                    android:inputType="textMultiLine"
                    android:maxLength="500"
                    android:importantForAutofill="no"
                    android:textColor="@color/text_primary"
                    android:textSize="15sp"/>

                <ImageButton
                    android:id="@+id/translate_copy_source"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:layout_gravity="top|end"
                    android:layout_margin="6dp"
                    android:src="@drawable/ic_copy"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:contentDescription="@string/translate_copy"
                    app:tint="@color/text_tertiary"/>

                <TextView
                    android:id="@+id/translate_input_counter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="bottom|end"
                    android:layout_margin="10dp"
                    android:textColor="@color/text_tertiary"
                    android:textSize="11sp"
                    tools:text="40/500"/>
            </FrameLayout>

            <Button
                android:id="@+id/translate_action"
                style="@style/Widget.Falcon.Button.Primary"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/gutter_small"
                android:text="@string/translate_action"/>

            <!-- Output -->
            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="@dimen/gutter_small"
                android:background="@drawable/bg_surface_card">

                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"
                    android:padding="14dp">

                    <LinearLayout
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"
                        android:gravity="center_vertical">

                        <TextView
                            android:id="@+id/translate_output_lang"
                            android:layout_width="0dp"
                            android:layout_height="wrap_content"
                            android:layout_weight="1"
                            android:textColor="@color/text_secondary"
                            android:textSize="13sp"
                            tools:text="Chinese"/>

                        <ImageButton
                            android:id="@+id/translate_copy_output"
                            android:layout_width="32dp"
                            android:layout_height="32dp"
                            android:src="@drawable/ic_copy"
                            android:background="?attr/selectableItemBackgroundBorderless"
                            android:contentDescription="@string/translate_copy"
                            app:tint="@color/text_tertiary"/>
                    </LinearLayout>

                    <TextView
                        android:id="@+id/translate_output"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="8dp"
                        android:minHeight="90dp"
                        android:textIsSelectable="true"
                        android:textColor="@color/text_primary"
                        android:textSize="16sp"/>

                    <TextView
                        android:id="@+id/translate_note"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_marginTop="6dp"
                        android:textColor="@color/status_warn"
                        android:textSize="11sp"
                        android:visibility="gone"
                        tools:text="via English"
                        tools:visibility="visible"/>
                </LinearLayout>
            </FrameLayout>
        </LinearLayout>
    </ScrollView>

    <include
        android:id="@+id/translate_busy"
        layout="@layout/view_busy"
        android:layout_width="match_parent"
        android:layout_height="match_parent"/>
</LinearLayout>
