// Top-level build file. Module config lives in app/build.gradle.
plugins {
    id 'com.android.application' version '8.13.0' apply false
}

tasks.register('clean', Delete) {
    delete rootProject.layout.buildDirectory
}
